use bevy::prelude::*;

/// Стили для UI элементов.
///
/// TODO: Будет реализовано в Phase 5.
#[derive(Clone, Debug, Resource)]
pub struct ButtonStyle;

impl Default for ButtonStyle {
    fn default() -> Self {
        Self
    }
}
